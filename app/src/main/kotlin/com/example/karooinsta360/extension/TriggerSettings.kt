package com.example.karooinsta360.extension

// Superseded (2026-08-27) by per-camera trigger config in
// com.example.karooinsta360.camera.CameraStore, once multi-camera support and
// independent heart-rate/power/speed triggers per camera were added. This global,
// single-metric settings object is no longer referenced anywhere; kept only as a
// placeholder so old comments/history pointing at "TriggerSettings" still resolve to
// something. Safe to delete once you no longer need that trail.
